apply plugin: 'java-library'

ext.moduleName = 'cph.crypto.persistence'

dependencies {
	implementation (
		project(':core'),
		[group: 'org.springframework.data', name: 'spring-data-mongodb', version: spring_data_mongo_version],
	)
}
