apply plugin: 'java-library'

ext.moduleName = 'cph.crypto.uuid'

dependencies {
	implementation (
		project(':core'),
		[group: 'com.fasterxml.uuid', name: 'java-uuid-generator', version: java_uuid_generator_version],
	)
}
